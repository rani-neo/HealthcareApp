import React from "react";
import { Alert } from "react-bootstrap";

const S3Uploader: React.FC = () => {
  return (
    <Alert variant="info" className="mt-3">
      S3 photo upload component can be added here later.
    </Alert>
  );
};

export default S3Uploader;